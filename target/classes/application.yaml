server:
  port: 8888  # 端口号

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/his?useSSL=false
    username: root
    password: 123456
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

logging:
      level:
        com.neuedu.mapper: debug

#mybatis-plus:
#  mapper-locations: classpath*:/mapper/*.xml